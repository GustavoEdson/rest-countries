import { Input } from "../Input";
import { Switch } from "../Switch";

export const Nav: React.FC = () => {
  return (
    <header className="flex justify-between p-20 flex-wrap text-2xl color-primary">
      <nav>Where in the world?</nav>
      <div>Dark mod</div>
    </header>
  );
};
